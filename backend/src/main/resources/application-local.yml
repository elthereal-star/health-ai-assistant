spring:
  sql:
    init:
      mode: always
  datasource:
    url: jdbc:mysql://localhost:3306/health_ai?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: root

ai:
  api-key: ${AI_API_KEY:}
  enabled: true

jwt:
  secret: ${JWT_SECRET:health-ai-assistant-local-secret-key}
  expiration: 86400000
